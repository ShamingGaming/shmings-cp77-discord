name: Update Discord Classnames
on:
  schedule:
    - cron: '0 0 * * *' # Jeden Tag um Mitternacht
  workflow_dispatch: # Für den manuellen Start

jobs:
  update:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v3
      
      # HIER IST DER FIX: Wir nutzen DTM450 statt vanyle
      - name: Run Update Script
        uses: DTM450/discord-update-classnames@master
        with:
          files: 'cp77.css, cp77_2.css'
          
      - name: Commit changes
        uses: stefanzweifel/git-auto-commit-action@v4
        with:
          commit_message: "Discord Update erkannt - Skins repariert"
